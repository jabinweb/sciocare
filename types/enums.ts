export enum UserRole {
  ADMIN = 'admin',
  MEMBER = 'member',
  TEACHER = 'teacher',
  STUDENT = 'student'
}

export enum RequestStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected'
}
